# =========================================================================
# 1. APPLICATION & SERVER CORE CONFIG
# =========================================================================
spring.application.name=postura

# ??? ??? ?? ??
server.port=8080
# ?? ???? ??????? ????? ?? (AWS ???? ??)
server.address=0.0.0.0

# AWS ALB/CloudFront ??? ?? ?? ?? (?? ??)
# ? ??? ?? Spring Security? ??? HTTPS? ?????.
server.forward-headers-strategy=FRAMEWORK


# =========================================================================
# 2. DATABASE (MySQL) CONFIG
# =========================================================================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# ?? ?? SPRING_DATASOURCE_URL? ??? ?? ??? ??
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/postura}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:20011109}


# =========================================================================
# 3. JPA / HIBERNATE CONFIG
# =========================================================================
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect


# =========================================================================
# 4. REDIS (ELASTICACHE) CONFIG
# =========================================================================
# ?? ?? SPRING_REDIS_HOST/PORT? ??? AWS ElastiCache ?? ????? ??
spring.data.redis.host=${SPRING_REDIS_HOST:master.postura.7o1tpz.apse2.cache.amazonaws.com}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}


# =========================================================================
# 5. JWT CONFIG (Placeholder Resolution Exception ??)
# =========================================================================
# JWT ???. ?? ?? JWT_SECRET? ??? ?? ??? ??
jwt.secret=${JWT_SECRET:local-jwt-secret-key}

# ? Placeholder Resolution Exception ??? ?? Placeholder ??? ??
# Access Token ?? ?? (???: 1800000ms = 30?)
jwt.access-token-expiration-in-milliseconds=${JWT_ACCESS_EXPIRATION:3600000}
# Refresh Token ?? ?? (???: 604800000ms = 7?)
jwt.refresh-token-expiration-in-milliseconds=${JWT_REFRESH_EXPIRATION:604800000}


# =========================================================================
# 6. OAuth2 CLIENT CONFIG (HTTPS ? ?? ?? ??)
# =========================================================================

# --- 1. Google ?? ---
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,profile,email
# [?? ??] HTTPS ? ?? ??
spring.security.oauth2.client.registration.google.redirect-uri=https://api.taba-postura.com/login/oauth2/code/google

# --- 2. Kakao ?? ---
spring.security.oauth2.client.registration.kakao.client-id=${KAKAO_CLIENT_ID}
spring.security.oauth2.client.registration.kakao.client-secret=${KAKAO_CLIENT_SECRET}
spring.security.oauth2.client.registration.kakao.authorization-grant-type=authorization_code
# [?? ??] HTTPS ? ?? ??
spring.security.oauth2.client.registration.kakao.redirect-uri=https://api.taba-postura.com/login/oauth2/code/kakao
spring.security.oauth2.client.registration.kakao.client-authentication-method=client_secret_post

# --- 3. Kakao Provider Endpoint ?? (?? ??) ---
spring.security.oauth2.client.provider.kakao.authorization-uri=https://kauth.kakao.com/oauth/authorize
spring.security.oauth2.client.provider.kakao.token-uri=https://kauth.kakao.com/oauth/token
spring.security.oauth2.client.provider.kakao.user-info-uri=https://kapi.kakao.com/v2/user/me
spring.security.oauth2.client.provider.kakao.user-name-attribute=id


# =========================================================================
# 7. APP CUSTOM PROPERTIES (OAuth2 ?? ????? URI)
# =========================================================================
app.oauth2.authorized-redirect-uri=https://taba-postura.com/oauth/redirect